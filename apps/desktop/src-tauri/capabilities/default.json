{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "main-window",
  "description": "Minimal capability for the main window. Deny-by-default: grows only with explicit review. Message frames get no capability at all. Notifications are listed because the window asks the OS to post them; the OS still decides.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default"
  ]
}
